echo "name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd backend && docker build -t slab-backend:prod -f Dockerfile.prod ." > .github/workflows/build.yml
