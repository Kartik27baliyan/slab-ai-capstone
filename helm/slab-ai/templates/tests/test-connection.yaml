apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slab-ai-chart.fullname" . }}-test-connection"
  labels:
    {{- include "slab-ai-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slab-ai-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
